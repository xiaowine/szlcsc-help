class CouponApp{constructor(){this.data=null;this.categories=null;this.brandCount=0;this.modal=null;this.categoryContainer=null}async init(){try{await this.loadRunTime();await this.loadCouponData();this.setupEventListeners();this.initializeComponents()}catch(error){console.error('初始化失败:',error);document.getElementById('loading').innerHTML='<p>数据加载失败，请稍后重试</p>'}}async loadRunTime(){const timeResponse=await fetch('run_time.txt');const timeContent=await timeResponse.text();document.getElementById('run_time').textContent=`更新时间：${timeContent}`}async loadCouponData(){const response=await fetch('coupon_details.json');this.data=await response.json();this.categories=Object.keys(this.data);const brandNameSet=new Set();for(const category of this.categories){for(const coupon of this.data[category]){brandNameSet.add(coupon['brand_name'])}}this.brandCount=brandNameSet.size;document.getElementById('loading').style.display='none';document.getElementById('count').textContent=`品牌数：${this.brandCount}，分类数：${this.categories.length}`}setupEventListeners(){this.setupModalEvents();this.setupBackToTopButton();this.setupSearchFeature()}setupModalEvents(){const infoModal=document.getElementById('infoModal');const showInfoBtn=document.getElementById('showInfo');const closeBtn=document.getElementById('closeModal');showInfoBtn?.addEventListener('click',()=>infoModal.style.display='flex');closeBtn?.addEventListener('click',()=>infoModal.style.display='none');infoModal?.addEventListener('click',(e)=>{if(e.target===infoModal)infoModal.style.display='none'})}setupBackToTopButton(){const backToTopButton=document.getElementById('backToTop');const container=document.querySelector('.container');container?.addEventListener('scroll',()=>{backToTopButton.style.display=container.scrollTop>300?'flex':'none'});backToTopButton?.addEventListener('click',()=>{container.scrollTo({top:0,behavior:'smooth'})})}filterCategories(searchTerm){const categoryLinks=document.querySelectorAll('.category-nav a');if(!searchTerm){categoryLinks.forEach(link=>link.style.display='block');return}if(!this.categoryContainer.classList.contains('category_active')){this.categoryContainer.classList.add('category_active')}categoryLinks.forEach(link=>{const linkText=link.textContent.toLowerCase();if(linkText.includes(searchTerm)){link.style.display='block'}else{link.style.display='none'}})}setupSearchFeature(){const searchToggle=document.getElementById('searchToggle');const searchClose=document.getElementById('searchClose');const searchContainer=document.querySelector('.search-container');const searchBox=document.getElementById('categorySearch');const openSearch=()=>{searchContainer.style.display='flex';searchContainer.offsetHeight;searchContainer.classList.add('active_search');setTimeout(()=>searchBox.focus(),300)};const closeSearch=()=>{searchContainer.classList.remove('active_search');setTimeout(()=>searchContainer.style.display='none',300);searchBox.value='';this.filterCategories('');searchBox.blur()};searchToggle?.addEventListener('click',(e)=>{e.preventDefault();searchContainer.classList.contains('active_search')?closeSearch():openSearch()});searchClose?.addEventListener('click',(e)=>{e.preventDefault();closeSearch()});searchBox?.addEventListener('input',(e)=>{this.filterCategories(e.target.value.trim().toLowerCase())});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'){if(searchContainer.classList.contains('active_search'))closeSearch();if(this.modal?.style.display==='flex')this.modal.style.display='none'}})}initializeComponents(){this.initializeCategoryNav();this.initializeCouponGrid()}initializeCategoryNav(){const nav=document.getElementById('category-nav');this.categoryContainer=this.createCategoryContainer();nav.appendChild(this.categoryContainer)}createCategoryContainer(){const container=document.createElement('div');container.className='category-container';const header=document.createElement('div');header.className='category-header';header.innerHTML=`<h2>分类列表<span class="category-count">(${this.categories.length}个分类)</span></h2><i class="fas fa-chevron-down toggle-icon"></i>`;const content=document.createElement('div');content.className='category-content';const categoryNav=this.createCategoryNav();content.appendChild(categoryNav);container.appendChild(header);container.appendChild(content);header.addEventListener('click',()=>{container.classList.toggle('category_active')});return container}createCategoryNav(){const nav=document.createElement('div');nav.className='category-nav';this.categories.forEach(category=>{const link=document.createElement('a');link.href=`#${category}`;link.innerHTML=`${category}<small>(${this.data[category].length})</small>`;link.addEventListener('click',(e)=>this.handleCategoryClick(e,category));nav.appendChild(link)});return nav}handleCategoryClick(e,category){e.preventDefault();this.categoryContainer.classList.remove('category_active');const targetCard=document.getElementById(category);targetCard.scrollIntoView({behavior:'smooth',block:'center'});targetCard.classList.add('highlight');document.querySelectorAll('.category-card.highlight').forEach(card=>{if(card!==targetCard)card.classList.remove('highlight')})}initializeCouponGrid(){const grid=document.getElementById('coupon-grid');this.modal=document.createElement('div');this.modal.className='coupon-modal';document.body.appendChild(this.modal);const sortedCategories=this.getSortedCategories();sortedCategories.forEach(category=>this.createCategoryCard(category,grid))}getSortedCategories(){return this.categories.sort((a,b)=>{const avgA=this.getAverageReceiveNum(this.data[a]);const avgB=this.getAverageReceiveNum(this.data[b]);return avgB-avgA})}getAverageReceiveNum(coupons){const total=coupons.reduce((sum,coupon)=>sum+coupon.receive_customer_num,0);return total/coupons.length}createCategoryCard(category,grid){const card=document.createElement('div');card.className='category-card';card.id=category;const avgReceiveNum=this.getAverageReceiveNum(this.data[category]);card.innerHTML=`<h2>${category}<br><span class="brand-count">共${this.data[category].length}个品牌<br>平均领取数:${avgReceiveNum.toFixed(0)}</span></h2>`;card.addEventListener('click',()=>this.showCouponModal(category,avgReceiveNum));grid.appendChild(card)}showCouponModal(category,avgReceiveNum){const sortedCoupons=this.data[category].sort((a,b)=>b.receive_customer_num-a.receive_customer_num);this.modal.innerHTML=this.createModalContent(category,avgReceiveNum,sortedCoupons);this.modal.style.display='flex';const closeBtn=this.modal.querySelector('.coupon-modal-close');closeBtn.addEventListener('click',()=>this.modal.style.display='none');this.modal.addEventListener('click',(e)=>{if(e.target===this.modal)this.modal.style.display='none'})}createModalContent(category,avgReceiveNum,coupons){return`<div class="coupon-modal-content"><div class="coupon-modal-header"><h2>${category}</h2>平均领取数:${avgReceiveNum.toFixed(0)}<button class="coupon-modal-close">&times;</button></div><div class="coupon-modal-body"><ul class="coupon-list">${coupons.map(coupon=>`<li class="coupon-item"><a href="https://list.szlcsc.com/brand/${coupon.brand_id}.html"target="_blank">${coupon.brand_name}-${coupon.coupon_name}<small>(优惠后最低消费${coupon.min_order_after_discount}元)</small><small>-已领取${coupon.receive_customer_num}张</small></a></li>`).join('')}</ul></div></div>`}}document.addEventListener('DOMContentLoaded',()=>{const app=new CouponApp();app.init()});